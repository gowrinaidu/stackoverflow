 <div class="top-sub-wrap">
        <%= image_tag("stack.jpg", :size => "65x65")%>
        Stack Flow
        <span style="font-size:14px; font-family:arial;"> We answer you </span>
        <span class="current-admin">
                <% if admin_signed_in? %>
                       You are logged in as:  <%= link_to current_admin.email, edit_admin_registration_path, :title=>"You want to edit", :style => 'color:white; text-decoration:none;' %>
                <%else%>
                        <%= link_to "Register Now",  new_admin_registration_path, :class=>"top-link" %>
                <%end%>
        </span>
 </div>

